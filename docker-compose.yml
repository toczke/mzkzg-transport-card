services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: ha-test
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./ha-test-config:/config:z
    environment:
      - TZ=Europe/Warsaw
